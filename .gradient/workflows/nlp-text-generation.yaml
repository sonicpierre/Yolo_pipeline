'on':
  github:
    branches:
      only: main
jobs:
  CloneRepo:
    resources:
      instance-type: C5
    outputs:
      repo:
        type: volume
    uses: git-checkout@v1
    with:
      url: context.event.github.url
  HelloWorld:
    resources:
      instance-type: C5
    needs:
      - CloneRepo
    uses: script@v1
    with:
      script: python3 train.py --img 640 --cfg yolov5s.yaml --hyp /inputs/repo/hyp.scratch.yaml --batch 8 --epochs 100 --data /inputs/repo/thumb.yaml --weights yolov5s.pt --workers 24 --name thumb
      image: ultralytics/yolov5
    inputs:
      repo: CloneRepo.outputs.repo
      demo-dataset:
        type: dataset
        with:
          ref: ds4r0a0xnoizszj
    outputs:
      my-volume:
        type: volume